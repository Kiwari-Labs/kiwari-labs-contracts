export const run = async () => {
    describe("Expiration", async function () {
      it("[HAPPY] the expiration period is correct when initialed", async function () {
        // TODO: add test case (suitable logic and event response).
      });

      it("[HAPPY] the expiration period is correct when the era passed", async function () {
        // TODO: add test case (suitable logic and event response).
      });
      
      it("[UNHAPPY] the expiration period is not suitable or impossible", async function () {
        // TODO: add test case (suitable logic and event response).
      });
    });
  };
  