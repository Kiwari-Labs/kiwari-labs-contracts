export const run = async () => {
  describe("Expiration", async function () {
    it("[SUCCESS] the whitelist address balance not expire when the epoch passed", async function () {
      // TODO: add test case (suitable logic and event response).
    });
  });
};
